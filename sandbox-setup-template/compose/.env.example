# Claude Sandbox Environment Configuration
# Copy to .env and customize

# ============================================
# REQUIRED: API Configuration
# ============================================

# Anthropic API key (required if not using Secret Manager)
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# OR: Secret Manager secret name (will fetch key at container start)
# API_KEY_SECRET=anthropic-api-key

# ============================================
# OPTIONAL: Git Configuration
# ============================================

# Git user for commits made by agents
GIT_USER_NAME=Claude Agent
GIT_USER_EMAIL=claude@example.com

# GitHub personal access token (for gh cli, PR creation, etc.)
# Create at: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_xxxxx

# Repository to clone into agent workspaces (optional)
# REPO_URL=https://github.com/your-org/your-repo.git

# ============================================
# OPTIONAL: Container Configuration
# ============================================

# Agent container image
# Default: claude-sandbox-agent:latest
# For Artifact Registry: us-central1-docker.pkg.dev/PROJECT/claude-sandbox/agent:latest
AGENT_IMAGE=claude-sandbox-agent:latest

# Number of agents (used by start-agents.sh)
AGENT_COUNT=12

# ============================================
# OPTIONAL: GCP Configuration
# ============================================

# GCP project ID (auto-detected on GCE VMs)
# GOOGLE_CLOUD_PROJECT=your-project-id

# Service account key file (not needed on GCE with workload identity)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/key.json
