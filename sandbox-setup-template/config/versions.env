# Claude Sandbox - Language and Tool Versions
# ============================================
#
# This file defines pinned versions for all languages and tools installed
# on the sandbox VM. Update these values to upgrade tooling.
#
# The provision-vm.sh script sources this file and uses these variables.
#
# Version Policy:
# - Pin MAJOR.MINOR for languages (allows patch updates)
# - Pin exact versions for build tools where reproducibility matters
# - Use "latest" or "stable" for tools that auto-update safely
#
# Last reviewed: 2025-01-25
# Based on: discord-bot-test-suite CI requirements

# ============================================
# Language Runtimes
# ============================================

# Node.js - used for Claude Code, Express, Fastify, and npm-based linting tools
# Matches: services/node-express, services/typescript-fastify Dockerfiles
NODE_VERSION="24"

# Python - used for Flask, Django, and Python-based linting (ruff, yamllint)
# Matches: services/python-flask, services/python-django Dockerfiles
PYTHON_VERSION="3.11"

# Go - used for Gin service and contract tests
# Matches: services/go-gin, tests/contract Dockerfiles
GO_VERSION="1.25.3"

# Rust - used for Actix service
# Matches: services/rust-actix Dockerfile
RUST_VERSION="1.93"

# Java (Eclipse Temurin) - used for Spring, Micronaut, Quarkus, Kotlin, Scala
# Matches: services/java-*, services/kotlin-ktor, services/scala-play Dockerfiles
JAVA_VERSION="21"

# Ruby - used for Rails service
# Matches: services/ruby-rails Dockerfile
RUBY_VERSION="3.4"

# PHP - used for Laravel service
# Matches: services/php-laravel Dockerfile
PHP_VERSION="8.5"

# .NET SDK - used for ASP.NET services
# Matches: services/csharp-aspnet, services/csharp-aspnet-native Dockerfiles
DOTNET_VERSION="10.0"

# Scala - used for Play service
# Note: Installed via sbt, this is for reference
SCALA_VERSION="3.3"

# ============================================
# Build Tools
# ============================================

# Gradle - used for Kotlin/Ktor service
GRADLE_VERSION="8.12"

# sbt - used for Scala/Play service
SBT_VERSION="1.10.6"

# Maven - used for Java services (bundled version via apt is usually fine)
# MAVEN_VERSION="3.9"  # Use system default

# Composer - PHP package manager (installer handles version)
# COMPOSER_VERSION="2"  # Use latest v2

# ============================================
# Linting & Code Quality Tools
# ============================================

# golangci-lint - Go linting (pre-commit: v2.8.0)
GOLANGCI_LINT_VERSION="v2.8.0"

# git-delta - syntax-highlighted git diffs (Anthropic devcontainer: 0.18.2)
DELTA_VERSION="0.18.2"

# Ruff - Python linting/formatting (pre-commit: v0.9.6)
RUFF_VERSION="0.9.6"

# actionlint - GitHub Actions linting (pre-commit: v1.7.7)
ACTIONLINT_VERSION="1.7.7"

# hadolint - Dockerfile linting (pre-commit: v2.12.0)
HADOLINT_VERSION="2.12.0"

# shellcheck - Shell script linting (pre-commit: v0.9.0.6)
SHELLCHECK_VERSION="0.9.0"

# ============================================
# Container & Cloud Tools
# ============================================

# Kubernetes - kubectl version
KUBECTL_VERSION="1.30"

# Terraform - infrastructure as code
TERRAFORM_VERSION="1.9"

# ============================================
# Drogon (C++) - Framework for cpp-drogon service
# ============================================
DROGON_VERSION="1.9.11"

# ============================================
# Node.js Global Packages (npm)
# ============================================
# These are installed via npm and pinned separately
# See package versions in provision-vm.sh npm install commands:
# - @anthropic-ai/claude-code (latest)
# - prettier (from pre-commit: v3.1.0)
# - cspell (latest)
# - markdownlint-cli2 (latest)
# - eslint (latest)
# - typescript (latest)
